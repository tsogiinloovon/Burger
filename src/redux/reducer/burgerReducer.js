const initialState = {
  ingredients: {
    salad: 1,
    becon: 2,
    cheese: 2,
    meat: 1,
  },
  totalPrice: 15000,
};

const burgerReducer = (state = initialState, action) => {
  return state;
};

export default burgerReducer;
